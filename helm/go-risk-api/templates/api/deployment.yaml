{{- if .Values.api.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    reloader.stakater.com/auto: "true"
  labels:
    app.kubernetes.io/name: {{ include "application.fullname" . }}
    app.kubernetes.io/component: {{ include "application.fullname" . }}-api
    environment: {{ .Values.environment }}
    release: {{ .Release.Name }}
  name: {{ include "application.fullname" .}}-api
spec:
  replicas: {{ .Values.api.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "application.fullname" . }}
      app.kubernetes.io/component: {{ include "application.fullname" . }}-api
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "application.fullname" . }}
        app.kubernetes.io/component: {{ include "application.fullname" . }}-api
    spec:
      containers:
        - name: {{ include "application.fullname" . }}-api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          args:
            - bash
            - -c
            - /code/scripts/run_prod.sh
          envFrom:
            - configMapRef:
                name: {{ include "application.fullname" . }}-api-configmap
            - secretRef:
                name: {{ include "application.secretname" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.api.containerPort }}
              protocol: TCP
          resources:
            {{- toYaml .Values.api.resources | nindent 12 }}
          volumeMounts:
            - name: {{ include "application.secretname" . }}
              mountPath: /mnt/secrets-store
              readOnly: true
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      restartPolicy: Always
      serviceAccountName: {{ include "application.serviceAccountName" . }}
      volumes:
        - name: {{ include "application.secretname" . }}
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: {{ include "application.fullname" . }}-secret-provider
{{- end }}